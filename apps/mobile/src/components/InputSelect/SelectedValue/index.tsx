import { SelectCompound, type SelectValueProps } from '@/design-system/components/SelectCompound'

export const SelectedValue = (props: SelectValueProps) => {
	return <SelectCompound.Value {...props} />
}
